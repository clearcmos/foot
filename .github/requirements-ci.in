# CI-only tools. Update versions deliberately, then regenerate with:
# uv pip compile .github/requirements-ci.in --universal --generate-hashes \
#   --output-file .github/requirements-ci.txt
codespell==2.4.2
gcovr==8.6
mypy==2.3.0
ruff==0.15.20
